name: Arduino CI

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Display Repository Information
        run: |
          echo "IIT Jammu Summer School 2026"
          echo "Repository checked successfully."
